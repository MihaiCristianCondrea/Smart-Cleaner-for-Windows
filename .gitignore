# ---- OS cruft ---------------------------------------------------------------
.DS_Store
Thumbs.db
ehthumbs.db
$RECYCLE.BIN/

# ---- Editors / IDEs ---------------------------------------------------------
# Visual Studio
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
# VS database/caches
*.VC.db
*.opendb
ipch/

# JetBrains Rider
.idea/
*.sln.iml
_ReSharper.*/
*.[Rr]e[Ss]harper

# VS Code (keep workspace settings if you like)
.vscode/*
!.vscode/settings.json

# ---- .NET / Build output ----------------------------------------------------
**/bin/
**/obj/
**/artifacts/
**/TestResults/
*.pidb
*.ipch

# Intermediate / compiler
*.ilk
*.pdb
*.pch
*.obj
*.iobj
*.log
*.tlog
*.tmp
*.cache
*.cachefile

# Roslyn/IDE caches
*.ide/

# ---- NuGet / restore --------------------------------------------------------
# packages folder (restore will repopulate)
**/packages/
# NuGet v3 assets
project.lock.json
project.assets.json
# nupkgs you might create locally
*.nupkg
.nuget/

# ---- Publish / distribution -------------------------------------------------
# Self-contained & framework-dependent publish directories
**/publish/
# Archives from scripts
*.zip
*.7z

# ---- MSIX / AppX packaging --------------------------------------------------
AppPackages/
BundleArtifacts/
**/*.msix
**/*.msixbundle
**/*.appx
**/*.appxbundle
**/*.appxsym
**/_pkginfo.txt

# Signing bits (never commit private keys/certs)
**/*.cer
**/*.pfx
**/*.p12

# ---- Logs / telemetry / diagnostics ----------------------------------------
logs/
**/logs/
*.log
*.etl
*.dmp
*.coverage
*.coveragexml
*.dotCover
coverage*/**

# ---- Node/yarn (if you generate assets with web tooling) -------------------
node_modules/
# lockfile optional; ignore if repo policy prefers reproducible restores via .csproj
# yarn.lock

# ---- Project-specific paths -------------------------------------------------
# Packaging images: keep the README that lists required sizes
Smart Cleaner for Windows/Packaging/Images/**
!Smart Cleaner for Windows/Packaging/Images/README.md

# Generated PRI sometimes ends up outside obj
*.pri

# Win32 .manifest (NOT the Package.appxmanifest)
# (WinUI 3 single-project uses Package.appxmanifest; this keeps stray .manifest out)
*.manifest
!src/SmartCleanerForWindows/app.manifest
!**/Package.appxmanifest

# ---- Secrets / signing / misc ----------------------------------------------
*.snk
*.key
*.keystore
*.keystore.jks

# ---- Optional: test runners & misc vendors ---------------------------------
# Chutzpah, NCrunch, etc. (add if you use them)
# _Chutzpah*
# _NCrunch_*
# .*crunch*.local.xml
