<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Smart_Cleaner_for_Windows.Shell.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dashboard="using:Smart_Cleaner_for_Windows.Modules.Dashboard.Views"
    xmlns:emptyViews="using:Smart_Cleaner_for_Windows.Modules.EmptyFolders.Views"
    xmlns:largeViews="using:Smart_Cleaner_for_Windows.Modules.LargeFiles.Views"
    xmlns:diskViews="using:Smart_Cleaner_for_Windows.Modules.DiskCleanup.Views"
    xmlns:internetViews="using:Smart_Cleaner_for_Windows.Modules.InternetRepair.Views"
    xmlns:settingsViews="using:Smart_Cleaner_for_Windows.Shell.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Smart Cleaner for Windows">

    <Grid>
        <NavigationView
            x:Name="RootNavigation"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="True"
            PaneDisplayMode="Left"
            CompactPaneLength="56"
            OpenPaneLength="260"
            IsPaneToggleButtonVisible="True"
            SelectionFollowsFocus="Disabled"
            AlwaysShowHeader="False"
            IsTitleBarAutoPaddingEnabled="False"
            SelectionChanged="OnNavigationSelectionChanged"
            Loaded="OnNavigationLoaded">

            <NavigationView.Resources>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,12" />
                    <Setter Property="Background" Value="Transparent" />
                </Style>
            </NavigationView.Resources>

        <NavigationView.MenuItems>
            <NavigationViewItem x:Name="DashboardItem" Content="Dashboard" IsSelected="True">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Home" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Name="EmptyFoldersItem" Content="Empty folders">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Folder" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Name="LargeFilesItem" Content="Large files">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="SaveLocal" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Name="DiskCleanupItem" Content="Disk cleanup">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Delete" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Name="InternetRepairItem" Content="Internet repair">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Globe" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

            <NavigationView.Content>
                <Grid>
                    <dashboard:DashboardView x:Name="DashboardView" />

                <emptyViews:EmptyFoldersView x:Name="EmptyFoldersView" Visibility="Collapsed" />


                <largeViews:LargeFilesView x:Name="LargeFilesView" Visibility="Collapsed" />


                <internetViews:InternetRepairView x:Name="InternetRepairView" Visibility="Collapsed" />


                <diskViews:DiskCleanupView x:Name="DiskCleanupView" Visibility="Collapsed" />


                <settingsViews:SettingsView x:Name="SettingsView" Visibility="Collapsed" />

            </Grid>
        </NavigationView.Content>
    </NavigationView>
    </Grid>
</Window>
