<UserControl
    x:Class="SmartCleanerForWindows.Modules.EmptyFolders.Views.EmptyFoldersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:empty="using:SmartCleanerForWindows.Modules.EmptyFolders.ViewModels"
    mc:Ignorable="d">

                <ScrollViewer
                    HorizontalScrollBarVisibility="Disabled">
                    <Grid Padding="{StaticResource Padding.Page}" RowSpacing="{StaticResource Spacing.Size240}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border
                            x:Name="StatusHero" x:FieldModifier="internal"
                            Grid.Row="0"
                            Background="{StaticResource Brush.Hero.Neutral}"
                            CornerRadius="28"
                            Padding="{StaticResource Padding.Hero}">
                            <Grid ColumnSpacing="{StaticResource Spacing.Size240}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border
                                    Width="76"
                                    Height="76"
                                    CornerRadius="24"
                                    Background="{StaticResource HeroCardOverlayBrush}"
                                    VerticalAlignment="Center">
                                    <SymbolIcon
                                        x:Name="StatusGlyph" x:FieldModifier="internal"
                                        Symbol="Folder"
                                        Foreground="White"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center" />
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="{StaticResource Spacing.Size80}">
                                    <TextBlock
                                        Text="Cleanup overview"
                                        Foreground="#B3FFFFFF"
                                        FontWeight="SemiBold" />
                                    <TextBlock
                                        x:Name="StatusTitle" x:FieldModifier="internal"
                                        Text="Ready when you are"
                                        FontSize="30"
                                        FontWeight="SemiBold"
                                        Foreground="White"
                                        TextWrapping="Wrap" />
                                    <TextBlock
                                        x:Name="StatusDescription" x:FieldModifier="internal"
                                        Text="Select a folder to begin."
                                        Foreground="#E6FFFFFF"
                                        TextWrapping="WrapWholeWords" />
                                </StackPanel>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Spacing="{StaticResource Spacing.Size40}">
                                    <TextBlock Text="Current results" Foreground="#B3FFFFFF" />
                                <InfoBadge
                                    x:Name="ResultBadge" x:FieldModifier="internal"
                                    HorizontalAlignment="Right"
                                    Visibility="Collapsed" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Grid
                            x:Name="EmptyFoldersLayout"
                            Grid.Row="1"
                            ColumnSpacing="{StaticResource Spacing.Size240}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="EmptyFoldersPrimaryColumn" Width="*" />
                                <ColumnDefinition x:Name="EmptyFoldersSecondaryColumn" Width="360" />
                            </Grid.ColumnDefinitions>

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup>
                                    <VisualState x:Name="EmptyFoldersCompactState">
                                        <VisualState.StateTriggers>
                                            <AdaptiveTrigger MinWindowWidth="0" />
                                        </VisualState.StateTriggers>
                                        <VisualState.Setters>
                                            <Setter Target="EmptyFoldersSecondaryColumn.Width" Value="0" />
                                            <Setter Target="EmptyFoldersLayout.ColumnSpacing" Value="{StaticResource Spacing.Size160}" />
                                            <Setter Target="EmptyFoldersPrimaryPanel.(Grid.ColumnSpan)" Value="2" />
                                            <Setter Target="EmptyFoldersDetailsPanel.(Grid.Row)" Value="1" />
                                            <Setter Target="EmptyFoldersDetailsPanel.(Grid.Column)" Value="0" />
                                            <Setter Target="EmptyFoldersDetailsPanel.(Grid.ColumnSpan)" Value="2" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="EmptyFoldersMediumState">
                                        <VisualState.StateTriggers>
                                            <AdaptiveTrigger MinWindowWidth="840" />
                                        </VisualState.StateTriggers>
                                        <VisualState.Setters>
                                            <Setter Target="EmptyFoldersSecondaryColumn.Width" Value="320" />
                                            <Setter Target="EmptyFoldersLayout.ColumnSpacing" Value="{StaticResource Spacing.Size240}" />
                                            <Setter Target="EmptyFoldersPrimaryPanel.(Grid.ColumnSpan)" Value="1" />
                                            <Setter Target="EmptyFoldersDetailsPanel.(Grid.Row)" Value="0" />
                                            <Setter Target="EmptyFoldersDetailsPanel.(Grid.Column)" Value="1" />
                                            <Setter Target="EmptyFoldersDetailsPanel.(Grid.ColumnSpan)" Value="1" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="EmptyFoldersWideState">
                                        <VisualState.StateTriggers>
                                            <AdaptiveTrigger MinWindowWidth="1024" />
                                        </VisualState.StateTriggers>
                                        <VisualState.Setters>
                                            <Setter Target="EmptyFoldersSecondaryColumn.Width" Value="360" />
                                            <Setter Target="EmptyFoldersLayout.ColumnSpacing" Value="{StaticResource Spacing.Size240}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <StackPanel
                                x:Name="EmptyFoldersPrimaryPanel"
                                Grid.Column="0"
                                Spacing="{StaticResource Spacing.Size160}">
                                <Border Style="{StaticResource CardStyle}">
                                    <Grid RowSpacing="{StaticResource Spacing.Size200}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size120}">
                                            <Border
                                                Width="40"
                                                Height="40"
                                                CornerRadius="14"
                                                Background="{ThemeResource AccentFillColorTertiaryBrush}"
                                                VerticalAlignment="Center">
                                                <SymbolIcon
                                                    Symbol="Folder"
                                                    Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center" />
                                            </Border>
                                            <StackPanel>
                                                <TextBlock Text="Root folder" FontSize="22" FontWeight="SemiBold" />
                                                <TextBlock
                                                    Text="Pick a starting location to scan for empty directories."
                                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                            </StackPanel>
                                        </StackPanel>

                                        <StackPanel Grid.Row="1" Spacing="{StaticResource Spacing.Size160}">
                                            <Grid ColumnSpacing="{StaticResource Spacing.Size120}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox
                                                    x:Name="RootPathBox" x:FieldModifier="internal"
                                                    Grid.Column="0"
                                                    PlaceholderText="Select a root folder…"
                                                    TextChanged="RootPathBox_TextChanged" />
                                                <Button x:Name="BrowseBtn" x:FieldModifier="internal" Grid.Column="1" Click="OnBrowse">
                                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size60}">
                                                        <SymbolIcon Symbol="OpenFile" />
                                                        <TextBlock Text="Browse" />
                                                    </StackPanel>
                                                </Button>
                                            </Grid>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="{StaticResource Spacing.Size80}">
                                                <Button x:Name="PreviewBtn" x:FieldModifier="internal" Click="OnPreview">
                                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size60}">
                                                        <SymbolIcon Symbol="PreviewLink" />
                                                        <TextBlock Text="Preview" />
                                                    </StackPanel>
                                                </Button>
                                                <Button x:Name="DeleteBtn" x:FieldModifier="internal" Click="OnDelete" IsEnabled="False">
                                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size60}">
                                                        <SymbolIcon Symbol="Delete" />
                                                        <TextBlock Text="Clean now" />
                                                    </StackPanel>
                                                </Button>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel Spacing="{StaticResource Spacing.Size200}">
                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size120}">
                                            <Border
                                                Width="40"
                                                Height="40"
                                                CornerRadius="14"
                                                Background="{ThemeResource AccentFillColorTertiaryBrush}"
                                                VerticalAlignment="Center">
                                                <SymbolIcon
                                                    Symbol="Setting"
                                                    Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center" />
                                            </Border>
                                            <StackPanel>
                                                <TextBlock Text="Scan options" FontSize="22" FontWeight="SemiBold" />
                                                <TextBlock
                                                    Text="Customize how the cleaner behaves before you run a scan."
                                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                            </StackPanel>
                                        </StackPanel>

                                        <StackPanel Spacing="{StaticResource Spacing.Size160}">
                                            <CheckBox x:Name="RecycleChk" x:FieldModifier="internal" IsChecked="True">
                                                <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size80}">
                                                    <FontIcon Glyph="&#xE107;" FontFamily="Segoe MDL2 Assets" />
                                                    <TextBlock Text="Send deleted folders to Recycle Bin" />
                                                </StackPanel>
                                            </CheckBox>

                                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size120}" VerticalAlignment="Center">
                                                <SymbolIcon
                                                    Symbol="Sort"
                                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                                <TextBlock VerticalAlignment="Center" Text="Depth limit" />
                                                <NumberBox
                                                    x:Name="DepthBox" x:FieldModifier="internal"
                                                    Width="96"
                                                    Minimum="0"
                                                    Maximum="999"
                                                    Value="0" />
                                            </StackPanel>

                                            <StackPanel Spacing="{StaticResource Spacing.Size40}">
                                                <TextBlock Text="Exclusions" FontWeight="SemiBold" />
                                                <TextBox
                                                    x:Name="ExcludeBox" x:FieldModifier="internal"
                                                    PlaceholderText=".git; build/*; node_modules"
                                                    AcceptsReturn="False" />
                                                <TextBlock
                                                    Text="Use semicolons to separate names or wildcard patterns."
                                                    FontSize="12"
                                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <StackPanel
                                x:Name="EmptyFoldersDetailsPanel"
                                Grid.Row="0"
                                Grid.Column="1"
                                Spacing="{StaticResource Spacing.Size160}">
                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel Spacing="{StaticResource Spacing.Size160}">
                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size120}">
                                            <Border
                                                Width="40"
                                                Height="40"
                                                CornerRadius="14"
                                                Background="{ThemeResource AccentFillColorTertiaryBrush}"
                                                VerticalAlignment="Center">
                                                <SymbolIcon
                                                    Symbol="Clock"
                                                    Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center" />
                                            </Border>
                                            <TextBlock Text="Activity" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center" />
                                        </StackPanel>

                                        <TextBlock
                                            x:Name="ActivityText" x:FieldModifier="internal"
                                            Text="Waiting for the next action."
                                            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                            TextWrapping="Wrap" />

                                        <ProgressBar
                                            x:Name="Progress" x:FieldModifier="internal"
                                            Height="4"
                                            Visibility="Collapsed"
                                            IsIndeterminate="True" />

                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button x:Name="CancelBtn" x:FieldModifier="internal" Click="OnCancel" Visibility="Collapsed" IsEnabled="False">
                                                <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size60}">
                                                    <SymbolIcon Symbol="Cancel" />
                                                    <TextBlock Text="Cancel" />
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>

                                        <InfoBar x:Name="Info" x:FieldModifier="internal" IsOpen="False" />
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel Spacing="{StaticResource Spacing.Size120}">
                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size120}">
                                            <Border
                                                Width="40"
                                                Height="40"
                                                CornerRadius="14"
                                                Background="{ThemeResource AccentFillColorTertiaryBrush}"
                                                VerticalAlignment="Center">
                                                <SymbolIcon
                                                    Symbol="Favorite"
                                                    Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center" />
                                            </Border>
                                            <TextBlock Text="Cleaner highlights" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center" />
                                        </StackPanel>

                                        <StackPanel Spacing="{StaticResource Spacing.Size120}">
                                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size80}">
                                                <SymbolIcon Symbol="PreviewLink" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                                <TextBlock
                                                    Text="Preview empty directories before committing to a cleanup."
                                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                    TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size80}">
                                                <SymbolIcon Symbol="ProtectedDocument" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                                <TextBlock
                                                    Text="Recycle Bin support keeps removals safe."
                                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                    TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <SymbolIcon Symbol="Filter" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                                <TextBlock
                                                    Text="Exclude directories with custom rules to stay in control."
                                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                    TextWrapping="Wrap" />
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                            <Grid RowSpacing="{StaticResource Spacing.Size160}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <Border
                                        Width="40"
                                        Height="40"
                                        CornerRadius="14"
                                        Background="{ThemeResource AccentFillColorTertiaryBrush}"
                                        VerticalAlignment="Center">
                                        <SymbolIcon
                                            Symbol="Preview"
                                            Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center" />
                                    </Border>
                                    <TextBlock Text="Preview results" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center" />
                                </StackPanel>

                                <TextBlock
                                    Grid.Row="1"
                                    x:Name="ResultsCaption" x:FieldModifier="internal"
                                    Text="Preview results will appear here once you run a scan."
                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />

                                <Grid Grid.Row="2" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox
                                        x:Name="ResultsSearchBox" x:FieldModifier="internal"
                                        Grid.Column="0"
                                        PlaceholderText="Search results…"
                                        TextChanged="OnResultSearchChanged"
                                        Margin="0"
                                        VerticalAlignment="Center" />
                                    <ComboBox
                                        x:Name="ResultsSortBox" x:FieldModifier="internal"
                                        Grid.Column="1"
                                        Width="180"
                                        SelectionChanged="OnResultSortChanged"
                                        VerticalAlignment="Center">
                                        <ComboBoxItem Content="Name (A-Z)" Tag="NameAscending" IsSelected="True" />
                                        <ComboBoxItem Content="Name (Z-A)" Tag="NameDescending" />
                                        <ComboBoxItem Content="Deepest folders first" Tag="DepthDescending" />
                                    </ComboBox>
                                    <ToggleSwitch
                                        x:Name="HideExcludedToggle" x:FieldModifier="internal"
                                        Grid.Column="2"
                                        Header="Hide excluded"
                                        Toggled="OnHideExcludedToggled"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center" />
                                    <Button
                                        x:Name="ClearFiltersBtn" x:FieldModifier="internal"
                                        Grid.Column="3"
                                        Click="OnClearResultFilters"
                                        VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <SymbolIcon Symbol="Clear" />
                                            <TextBlock Text="Clear filters" />
                                        </StackPanel>
                                    </Button>
                                </Grid>

                                <Grid Grid.Row="3" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                        <Button x:Name="ExcludeSelectedBtn" x:FieldModifier="internal" Click="OnExcludeSelected">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <SymbolIcon Symbol="BlockContact" />
                                                <TextBlock Text="Exclude selected" />
                                            </StackPanel>
                                        </Button>
                                        <Button x:Name="IncludeSelectedBtn" x:FieldModifier="internal" Click="OnIncludeSelected">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <SymbolIcon Symbol="Accept" />
                                                <TextBlock Text="Include selected" />
                                            </StackPanel>
                                        </Button>
                                        <Button x:Name="ClearInlineExclusionsBtn" x:FieldModifier="internal" Click="OnClearInlineExclusions">
                                            <StackPanel Orientation="Horizontal" Spacing="6">
                                                <SymbolIcon Symbol="Redo" />
                                                <TextBlock Text="Clear inline exclusions" />
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                    <TextBlock
                                        Grid.Column="1"
                                        x:Name="InlineExclusionSummary" x:FieldModifier="internal"
                                        Text="No inline exclusions applied."
                                        HorizontalAlignment="Right"
                                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                        VerticalAlignment="Center" />
                                </Grid>

                                <TreeView x:Name="CandidatesTree" x:FieldModifier="internal"
          Grid.Row="4"
          SelectionMode="Multiple"
          SelectionChanged="OnCandidatesSelectionChanged">

    <TreeView.ItemTemplate>
        <!-- The x:DataType MUST be on the DataTemplate -->
        <DataTemplate x:DataType="empty:EmptyFolderNode">
            <!-- For hierarchical data, wrap your item in a TreeViewItem and bind its ItemsSource -->
            <TreeViewItem ItemsSource="{x:Bind Children}">
                <Grid ColumnSpacing="12"
                      Padding="8"
                      Background="{ThemeResource LayerFillColorDefaultBrush}"
                      CornerRadius="8"
                      ToolTipService.ToolTip="{x:Bind FullPath}"
                      Opacity="{x:Bind ExclusionOpacity, Mode=OneWay}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Border Width="32" Height="32" CornerRadius="12"
                            Background="{ThemeResource AccentFillColorTertiaryBrush}"
                            HorizontalAlignment="Center" VerticalAlignment="Center">
                        <SymbolIcon Symbol="Folder"
                                    Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                    </Border>

                    <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                        <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Bind RelativePath}"
                                   FontSize="12"
                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                    </StackPanel>

                    <StackPanel
                        Grid.Column="2"
                        Orientation="Horizontal"
                        Spacing="6"
                        VerticalAlignment="Center">
                        <InfoBadge
                            Value="{x:Bind PathLength}"
                            Visibility="{x:Bind IsPathLengthWarning, Converter={StaticResource BooleanToVisibilityConverter}}"
                            ToolTipService.ToolTip="{x:Bind PathLengthIndicatorTooltip}" />
                    </StackPanel>

                    <ToggleButton Grid.Column="3"
                                  Content="Exclude"
                                  IsChecked="{x:Bind IsDirectlyExcluded, Mode=TwoWay}"
                                  IsEnabled="{x:Bind IsInlineToggleEnabled}"
                                  VerticalAlignment="Center"
                                  ToolTipService.ToolTip="Exclude this folder from cleanup" />
                </Grid>
            </TreeViewItem>
        </DataTemplate>
    </TreeView.ItemTemplate>
</TreeView>

                            </Grid>
                        </Border>
                    </Grid>
                </ScrollViewer>
</UserControl>
