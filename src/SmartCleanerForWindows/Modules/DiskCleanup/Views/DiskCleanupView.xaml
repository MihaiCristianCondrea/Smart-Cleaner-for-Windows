<UserControl
    x:Class="SmartCleanerForWindows.Modules.DiskCleanup.Views.DiskCleanupView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

                <ScrollViewer
                    HorizontalScrollBarVisibility="Disabled">
                    <Grid Padding="{StaticResource Padding.Page}" RowSpacing="{StaticResource Spacing.Size240}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="{StaticResource Brush.Hero.Neutral}" CornerRadius="28" Padding="{StaticResource Padding.Hero}">
                            <Grid ColumnSpacing="{StaticResource Spacing.Size240}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Border
                                    Width="76"
                                    Height="76"
                                    CornerRadius="24"
                                    Background="{StaticResource HeroCardOverlayBrush}"
                                    VerticalAlignment="Center">
                                    <FontIcon
                                        Glyph="&#xE74D;"
                                        FontFamily="Segoe MDL2 Assets"
                                        Foreground="White"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center" />
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="{StaticResource Spacing.Size80}">
                                    <TextBlock Text="Disk cleanup" Foreground="#B3FFFFFF" FontWeight="SemiBold" />
                                    <TextBlock
                                        x:Name="DiskCleanupIntro" x:FieldModifier="internal"
                                        Text="Analyze Windows cleanup handlers to reclaim temporary files."
                                        Foreground="#E6FFFFFF"
                                        TextWrapping="Wrap" />
                                    <TextBlock
                                        x:Name="DiskCleanupStatusText" x:FieldModifier="internal"
                                        Text="Ready to analyze Windows disk cleanup handlers."
                                        Foreground="#E6FFFFFF"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                            <StackPanel Spacing="{StaticResource Spacing.Size160}">
                                <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size120}">
                                    <Border
                                        Width="40"
                                        Height="40"
                                        CornerRadius="14"
                                        Background="{ThemeResource AccentFillColorTertiaryBrush}"
                                        VerticalAlignment="Center">
                                        <FontIcon
                                            Glyph="&#xE74D;"
                                            FontFamily="Segoe MDL2 Assets"
                                            Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center" />
                                    </Border>
                                    <TextBlock Text="Cleanup categories" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center" />
                                </StackPanel>

                                <TextBlock
                                    x:Name="DiskCleanupActivityText" x:FieldModifier="internal"
                                    Text="Waiting for the next action."
                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                    TextWrapping="Wrap" />

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="{StaticResource Spacing.Size80}">
                                    <Button x:Name="DiskCleanupAnalyzeBtn" x:FieldModifier="internal" Click="OnDiskCleanupAnalyze">
                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size60}">
                                            <SymbolIcon Symbol="Refresh" />
                                            <TextBlock Text="Analyze" />
                                        </StackPanel>
                                    </Button>
                                    <Button x:Name="DiskCleanupCleanBtn" x:FieldModifier="internal" Click="OnDiskCleanupClean" IsEnabled="False">
                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size60}">
                                            <SymbolIcon Symbol="Delete" />
                                            <TextBlock Text="Clean selected" />
                                        </StackPanel>
                                    </Button>
                                    <Button x:Name="DiskCleanupCancelBtn" x:FieldModifier="internal" Click="OnCancel" Visibility="Collapsed" IsEnabled="False">
                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource Spacing.Size60}">
                                            <SymbolIcon Symbol="Cancel" />
                                            <TextBlock Text="Cancel" />
                                        </StackPanel>
                                    </Button>
                                </StackPanel>

                                <ProgressBar
                                    x:Name="DiskCleanupProgress" x:FieldModifier="internal"
                                    Height="4"
                                    Visibility="Collapsed"
                                    IsIndeterminate="True" />

                                <InfoBar x:Name="DiskCleanupInfoBar" x:FieldModifier="internal" IsOpen="False" />

                                <ListView
                                    x:Name="DiskCleanupList" x:FieldModifier="internal"
                                    SelectionMode="None"
                                    IsItemClickEnabled="False">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox
                                                Padding="0"
                                                HorizontalContentAlignment="Stretch"
                                                IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                IsEnabled="{Binding CanSelect}">
                                                <Grid ColumnSpacing="12">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Border
                                                        Width="32"
                                                        Height="32"
                                                        CornerRadius="12"
                                                        Background="{ThemeResource AccentFillColorTertiaryBrush}"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center">
                                                        <FontIcon
                                                            Glyph="&#xE74D;"
                                                            FontFamily="Segoe MDL2 Assets"
                                                            Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center" />
                                                    </Border>
                                                    <StackPanel Grid.Column="1" Spacing="2">
                                                        <TextBlock
                                                            Text="{Binding Name}"
                                                            FontWeight="SemiBold"
                                                            TextWrapping="Wrap" />
                                                        <TextBlock
                                                            Text="{Binding Description}"
                                                            Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                            TextWrapping="Wrap" />
                                                        <TextBlock
                                                            Text="{Binding ErrorMessage}"
                                                            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                                            TextWrapping="Wrap" />
                                                    </StackPanel>
                                                    <TextBlock
                                                        Grid.Column="2"
                                                        Text="{Binding FormattedSize}"
                                                        HorizontalAlignment="Right"
                                                        VerticalAlignment="Center"
                                                        FontWeight="SemiBold" />
                                                </Grid>
                                            </CheckBox>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
</UserControl>
